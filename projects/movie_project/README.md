# Movie Collection Manager (C++)

A small C++ project that manages a list of movies using object‑oriented programming.  
Each movie stores a name, rating, and watched count.

## Features
- Add movies
- Prevent duplicate entries
- Increment watched count
- Display all movies

## Structure
- `Movie` class → represents one movie
- `Movies` class → stores a vector of Movie objects and manages them
