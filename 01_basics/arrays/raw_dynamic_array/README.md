📘 Dynamic Array (C++)
This folder contains a simple example demonstrating how to create, use, and free a raw dynamic array in C++ using new[] and delete[].
It’s part of the basics section of the repository and is meant to help build a solid foundation in pointers, memory allocation, and array indexing.
