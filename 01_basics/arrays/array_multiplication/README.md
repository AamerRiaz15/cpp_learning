# Array Multiplication (apply_all)

This program multiplies every element of one array with every element of another array.
It demonstrates:
- nested loops
- dynamic memory allocation
- returning pointers
- basic array operations
