Deep Copy Example in C++
This project demonstrates how deep copying works in C++ using a simple class that manages a dynamically allocated integer.

Deep copying is an essential concept when working with pointers. It ensures that each object owns its own separate memory, preventing issues like shared pointers, double deletes, and unexpected value changes.
